\chapter{Sorting routines}\label{chap:benchmark:sorting}

You should benchmark both stable and unstable array sorting routines, including the \texttt{qsort(3)} of the standard library and the \texttt{array\_heapsort()} from your binary heaps implementation in Chap.~\ref{chap:heaps}. 

It is perfectly possible to achieve (much) faster sorting than \texttt{qsort(3)}, albeit obviously with the same asymptotic complexity. Also while ensuring stability?

